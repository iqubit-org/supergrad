# Enhanced Multi-GPU Profiler Requirements
# Install with: pip install -r requirements_enhanced_profiler.txt

# Core dependencies
psutil>=5.8.0
pynvml>=11.0.0  # Optional: for GPU memory monitoring

# JAX and related (core requirements)
jax>=0.4.0
jaxlib>=0.4.0

# SuperGrad dependencies (required for real benchmarks)
qiskit-dynamics>=0.4.0
qutip>=4.7.0

# Note: These dependencies are required for the real benchmark functions
# from test_simultaneous_x.py that we're profiling
