# Enhanced Multi-GPU Profiler Requirements
# Install with: pip install -r requirements_enhanced_profiler.txt

# Core dependencies
psutil>=5.8.0
pynvml>=11.0.0

# JAX and related
jax>=0.4.0
jaxlib>=0.4.0

# Data processing
numpy>=1.21.0
pandas>=1.3.0

# Optional: For better GPU monitoring
GPUtil>=1.4.0

# Note: pynvml is required for GPU memory monitoring
# If not available, the profiler will run with limited GPU metrics
